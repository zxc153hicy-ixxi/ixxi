---
tags: [正模式]
status: active
confidence: high
summary: CLI工具不可用时自动检测Python API替代方案，修脚本打通链路，不每次手动绕过
created: 2026-07-14
updated: 2026-07-14
---

# 工具链自修复

## 场景
自动化脚本依赖外部 CLI 工具（如 `markitdown`、`pandoc`），但环境中 CLI 未加入 PATH，导致批量操作全部失败。

## 做法
1. 脚本启动时检查 CLI 可用性（`shutil.which()`）
2. CLI 不可用时，检测是否有等效 Python API（`try: from module import Class`）
3. 有 API → 自动降级走 API，标注实际使用的工具路径，记录实际调用方式
4. 无 API → 报告明确错误（附 `pip install xxx` 安装指引）
5. 降级逻辑内聚在脚本中，调用方无感

## 为什么有效
- **消除手动绕过**：不用每次临时写新脚本绕过坏掉的 CLI
- **单点修复全局生效**：修一次入口脚本，所有后续调用自动受益
- **可追溯**：转换结果中标记实际工具路径，出问题可回溯

## 实例
```python
_MARKITDOWN_CLI = shutil.which("markitdown")

if info["tool_name"] == "MarkItDown" and not _MARKITDOWN_CLI:
    success, stdout, stderr = _markitdown_api_convert(filepath, output_file)
```

## 关联
- [[技术强制实施]] —— 能写脚本强制的不靠人记
- [[钩子只做纯机械]] —— 环境检查属纯机械检测，可脚本化
- [[转换前预扫描]] —— 转换前先检测文件类型
