# docs/evolution/

「边界随使用演化」机制的公开规则（对应完整设计的 R-EVO 系列 9 条 + R-REG 系列 6 条）。

- **开放注册（R-REG 01-06）**：新实体开放注册 → 受控词表校验 → 频次驱动封装 → 复用封装 → 通用化晋升 → 贡献回流。
- **动态收敛（R-EVO 01-09）**：五阶段生命周期 → stale 软信号 → 零引用硬信号 → 规则去重 → frontmatter 生命周期 → 阈值自适应 → 主动沉积 → 特例降级 → 使用遥测纪律。

统一使用遥测 = 演化决策信号（usage ≠ value，价值判断保留人工裁决）。

> 内容待补：把上述规则从设计文档抽象为逐条公开规则（每条含编号 + 可操作步骤）。
