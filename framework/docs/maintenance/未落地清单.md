---
tags: [维护, 清单]
status: active
summary: ixxi 设计方案未落地清单——24 项已落地（2026-08-15 销账），剩 26 项（待环境 6/待真实使用 6/后置 14）
created: 2026-08-15
---

# 设计方案未落地清单

> 盘点日 2026-08-15，对照「设计文档承诺」vs「ixxi 实际状态」得出。**50 项**，供逐项销账。
> 销账方式：做完一项，把状态从 `[ ]` 改 `[x]`，注明日期。

## 统计与优先

| 类 | 数量 | 说明 |
|---|---|---|
| ✅ 已落地 | 24 | 2026-08-15 全部销账（见下方各表，逐项已改 `[x]`） |
| 待环境验证 | 6 | 需 GitHub/Codex/Hermes 真实环境 |
| 待真实使用 | 6 | 需作者实际使用积累 |
| 后置 | 14 | 开源前或真实场景到来时再做 |

**Top 3 优先**：① 修 pre-commit（护栏静默失效）② 能力可执行性验证（I2/I4）③ 演化飞轮 + telemetry（阶段五整段未启动）。

## 销账记录（2026-08-15）

- **24 项「现在可做」全部落地**（见「一、已落地」各表）：
  - A 组安全/机制地基 11 项、B 组能力契约/核心承诺 5 项、C 组低成本补全/清理 8 项。
  - 落地 commit：`c819092`（Top1 pre-commit）→ `615783c`（A 安全地基）→ `f1f9748`（A 机制）→ `f094361`（Top2 可执行性）→ `b8fec57`（Top3 演化）→ `9859fa2`（B 组）→ C 组清理。
  - 关键新增：`verify-capability.py`（I4 dry-run）、`record-usage.py`（telemetry）、`gen-resources.py`/`gen-lifecycle.py`（capability 补齐）、`supports.json`（I3）、`evolution-config.yaml`（阈值配置化）。
- **26 项分类核对确认**（2026-08-15 逐项过一遍）：
  - 待环境验证 6 项：均需 GitHub/Codex/Hermes 真实环境，本地无法完成，分类不变。
  - 待真实使用 6 项：均需作者实际使用积累（3-6 月），分类不变。
  - 后置 14 项：均属开源前 / 真实场景 / 生态演进触发，分类不变。

---

## 一、已落地（24 项，2026-08-15 销账）

### A. 安全/机制地基

| # | 项 | 未落地原因 | 建议 |
|---|---|---|---|
| 1 | 修 pre-commit 路径 bug | `.git/hooks/` 只有 .sample，护栏静默失效；engine/scripts 段缺 framework/ 前缀 | 修路径 + 激活钩子 |
| 2 | bandit High 清理 | auto-import.py 1 处 shell=True，CI 一跑就红 | 改 shell=False |
| 3 | 补打 git tag | 实测 tag 为空，baseline + v0.1 都没打 | 补打 ixxi-baseline + v0.1 |
| 4 | DCO 强制 | commit -s 无钩子约束 | pre-commit 加 sign-off 检查 |
| 5 | 补混淆代码恶意样例 | 6 类攻击面只有 5 个样例 | 补 sample-06 |
| 6 | 错误码接入三处 | 只接入 2 脚本，parity/sync/lint 零命中 | 按规范补错误码 |
| 7 | parity 挂 /check + pre-commit | 脚本已建未挂载 | 挂 /check + 委托链 |
| 8 | 硬闸门补全 enrich/lint | 纯流程型 skill 只覆盖 4 个 | 补 enrich/lint 硬闸门 |
| 9 | ixxi stats --unused CLI 接线 | 脚本有但 ixxi 只有 init 子命令 | 加 stats 子命令转发 |
| 10 | core/onboarding.json 补齐 | AGENT.md 已引用但文件不存在（断链） | 补空 onboarding.json |
| 11 | .agents/skills/README.md 补齐 | framework 内不存在，计数口径未对齐 | 建 README + 对齐 72 |

### B. 能力契约/核心承诺

| # | 项 | 未落地原因 | 建议 |
|---|---|---|---|
| 12 | 能力契约 schema 完善 | lifecycle_class 缺失（I9 无机器表达）；rule/agent/hook 无 version 校验；schema 未随 framework 发布；gen-capability.py 不存在 | 补 lifecycle_class + version 校验 + 发布 schema + gen 脚本 |
| 13 | capability resources 字段补齐 | 76 个全留空 []，parity P5 不读 resources | 补 resources |
| 14 | **能力可执行性验证（I2/I3/I4）** | parity 只证明「可达」不证明「能用」；无 tier、无 dry-run、无 agent.supports | 加 tier + dry-run 验证器 + supports 声明 |
| 15 | **演化飞轮 + telemetry（阶段五/I8）** | 唯一整阶段未启动；无 usage 采集/候选/人工确认/抽象工具化 | 搭 telemetry 采集 + 演化工具链 |
| 16 | 遥测隐私边界声明 | 读 personal 本地数据但无隐私声明 | 补声明文档 |

### C. 低成本补全/清理

| # | 项 | 未落地原因 | 建议 |
|---|---|---|---|
| 17 | 演化阈值配置化 | check_stale.py 阈值硬编码 90 天 | 读 engine/config/ |
| 18 | sync 脚本事务化回滚 | sync 半途失败无回滚 | 先备份再重建，失败还原 |
| 19 | PR 审查抽查样本 | 脱敏声明强制但抽查未实现 | 审查清单加一条抽查 |
| 20 | 抄袭检查 | 只 CLA 自动校验，无复制检查 | ci 加 git diff 原创性提示 |
| 21 | 判据三问勾选清单 | 判据文字化但不可操作 | 贡献流程做成勾选 |
| 22 | 根目录三件套复核 | 两路盘点口径矛盾（README/LICENSE/GETTING-STARTED 位置） | 实地复核补缺 |
| 23 | 顺手修文档措辞 | AGENT.md「mcp 待建」过期；定时任务等权声明缺失；无 MANUAL 区 | 改「待建」→「预留位」+ 补声明 |
| 24 | git 索引漂移清理 | kb-* 平铺 15 个 capability.json 仍 tracked | git rm --cached 清理 |

---

## 二、待环境验证（6 项）

| # | 项 | 依赖 |
|---|---|---|
| 1 | CI 真实运行（四件套 + clean-room） | GitHub Actions |
| 2 | CLA 机器人 + cla.yml 配置 | 真实仓库 + CLA_PAT |
| 3 | 真实 public remote 阻断实测 | 真实 remote |
| 4 | good first issue 标签 | GitHub 仓库 |
| 5 | 三 agent 产物行为实测（Codex/Hermes 各跑一遍 + hash 比对） | Codex/Hermes 环境 |
| 6 | Codex hooks 二选一实测 | Codex 环境 |

---

## 三、待真实使用（6 项）

| # | 项 | 依赖 |
|---|---|---|
| 1 | dogfooding 3-5 真实场景（**最大 gap，时间型**） | 作者日常使用 3-6 月 |
| 2 | 真实迁移切换（demo→真实三步） | 作者首次真实接入 |
| 3 | co-maintainer 招募（bus factor ≥2）+ 产能声明 | 开源后 |
| 4 | 判据外部信号（≥2 用户/star） | 开源有人用后 |
| 5 | MVP 边界真实演化验证 | 真实使用 |
| 6 | 演化飞轮真实数据积累 | 真实使用 |

---

## 四、后置（14 项）

| # | 项 | 触发时机 |
|---|---|---|
| 1 | 容器沙箱执行 | 开源前 |
| 2 | 供应链分级读码 + 禁止自合并 | 开源前（§9.2 硬门槛） |
| 3 | 白名单强制机制（v1.1） | 设计风险表原话 |
| 4 | 迁移 dry-run/rollback 行为验证 | 迁移场景到来 |
| 5 | 覆盖率 60% 实测 | CI 就绪后 |
| 6 | 版本号/发布/兼容矩阵 | 开源第一版 release 前；建立版本号机制后重新接入 version-check.sh（当前 suspended，见 hooks/registry.json） |
| 7 | A2A 协议演进对冲 | 生态演进 |
| 8 | parity 降级出口 | 真实多 agent 使用后 |
| 9 | personal 侧声明（skill 集参数化） | v1 后 |
| 10 | I5 路由消费 Capability ID | 随可执行性验证 |
| 11 | Instance 抽象实现 | 真实使用积累后 |
| 12 | Contribution 抽象实现 + README 补第四抽象 | 开源贡献场景 |
| 13 | MCP 实际接入（预留位已建） | 真实场景 |
| 14 | 定时任务三 agent 等权 | 功能启用时 |

---

## 关联

- [[维护者手册]] —— 目录地图 + 改什么跑什么 + 疏忽检查清单
- [[../guides/dogfooding案例]] —— 待真实使用项的依据（模拟案例）
