Invoke the `scene-brief` skill with these inputs:

- `PRIMARY_PATH`: `{STORY_PATH}`
- `SCENE_TAG`: `{SCENE_TAG}`
- `CONTEXT_PATHS`: `{PRIOR_SCENE_PATH}` (omit entirely if this is scene 1 / no prior scene was provided)

Do not read any files beyond what the skill itself directs you to read. Do not write anywhere except `output/`.

Report back the absolute path of the scene brief file written.
