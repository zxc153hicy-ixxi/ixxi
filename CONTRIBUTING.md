# 贡献指南（CONTRIBUTING）

> ixxi 是一个「Agent 能力可迁移、可验证、可演化」的开源中间层，采用 [MIT License](LICENSE)。
> 本文档说明如何为 ixxi 贡献，以及贡献时你必须了解的法律与流程要求。

## 1. 许可证

本项目采用 [MIT License](LICENSE)。你贡献到仓库的所有内容，将在 MIT 许可下向所有使用者开放。

## 2. 显式贡献流程（不变量 I7）

ixxi 的核心不变量 **I7「显式贡献流程」** 要求：任何内容从个人环境晋升到开源通用层，都必须走显式、可追溯的贡献流程，而不是悄悄复制粘贴。

贡献路径：

```
fork 仓库 → 创建分支 → 提交（git commit -s）→ 推送 → 发起 PR → 审查 → 合并
```

### 2.1 fork

在 GitHub 上点击 Fork，把 `ixxi` 复制到你的账号。

### 2.2 创建分支

从 `main` 创建功能分支，命名建议：`feat/<简述>`、`fix/<简述>`、`docs/<简述>`。

### 2.3 提交（必须带 DCO 签名）

所有提交必须使用 `-s`（`--signoff`）签名：

```bash
git commit -s
# 或
git commit --signoff
```

每个提交末尾会自动追加一行：

```
Signed-off-by: 你的名字 <你的邮箱>
```

**签名含义（DCO）**：签名表示你同意「Developer Certificate of Origin (DCO) 1.1」条款，即你证明：

- 该提交全部或部分由你创作，你有权在项目开源许可证（MIT）下提交；或
- 该提交基于你知情的、在合适开源许可下发布的前人作品，你有权在此基础上修改并提交；或
- 该提交由其他已做上述认证的人直接提供给你，你未做修改；
- 你理解并同意该提交及签名记录是公开的，且可能被长期保存并随项目按开源许可重新分发。

签名是**法律承诺**，不是形式。请只提交你有权提交的内容。若使用 GitHub 网页界面或图形化工具提交，请确认提交记录里带 `Signed-off-by` 行；缺失签名的提交会被审查阶段退回。

### 2.4 推送并发起 PR

推送到你的 fork，然后创建 Pull Request 到上游 `main` 分支。PR 描述请说明：改动目的、改动内容、验证方式。

### 2.5 审查

- **CLA 检查**：外部贡献者首次 PR 时，cla-assistant 机器人会在 PR 下自动留言，点击其中按钮签署 CLA（见第 3 节）。未签署前 PR 不放行。
- **DCO 检查**：所有提交必须带 `Signed-off-by` 签名，审查时核对。
- **审查团**：框架自带的审查 agent（质量 / 安全 / 架构等）与维护者共同审查。
- 审查通过后由维护者合并。

## 3. 贡献者许可协议（CLA）

**谁需要签**：所有非项目维护者的外部贡献者，首次提交 PR 时签署。

**签什么**：你授权 ixxi 项目在 MIT 许可下使用、复制、修改、分发你的贡献（含后续修改）。签署一次，按你的 GitHub 身份长期有效，后续贡献无需重复签署。

**怎么签**：cla-assistant 会在你的首个 PR 下自动留言，点击「I have read the CLA Document and I hereby sign the CLA」按钮即完成签署，无需人工干预。

**为什么要有 CLA**：保证项目对所有贡献拥有清晰、无争议的分发权，避免第三方内容未经授权进入仓库的法律风险。全文见 [CLA](.github/CLA.md)。

## 4. 晋升判据：「换个陌生使用者还有用吗」

贡献的内容要进入开源通用层 `framework/`，必须通过以下判据：

> **「换个陌生使用者还有用吗？」—— 有用，进 `framework/`；没用，留 `personal/`。**

- **有用**：内容对任何陌生使用者都具备可复用的通用价值（机制、schema、通用 skill、通用规则、通用教训）。
- **没用**：内容依赖你个人的环境、数据、偏好、故事，换个陌生使用者就失效，应留在私有实例层 `personal/`（不入库、不提交）。

只有通过该判据的内容才允许进入 `framework/` 目录。无法判断时，先在 PR 描述中说明判断过程，供审查团裁定。

## 5. 提交信息规范

- 格式：`<type>(<scope>): <subject>`
- type：`feat` / `fix` / `docs` / `refactor` / `chore`
- scope：影响区域，如 `framework`、`docs`、`init`
- 例：`feat(framework): 新增 X 机制`

## 6. 代码与文档规范

- 代码、标识符、schema 字段保留原文；注释与文档用中文。
- 可迁移对象需带 `version`（不变量 I10）。
- 改动需附验证结果（行为验证：改了计数就重新数、建了 skill 就确认出现在列表）。
